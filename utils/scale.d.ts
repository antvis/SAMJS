declare const handleImageScale: (image: HTMLImageElement) => {
    height: number;
    width: number;
    samScale: number;
};
declare const handleScale: (w: number, h: number) => {
    height: number;
    width: number;
    samScale: number;
};
export { handleImageScale, handleScale };
